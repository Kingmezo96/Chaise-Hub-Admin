import './pro-track.css';

export const metadata = {
  title: 'Chaise Pro Track',
  description: 'Super-admin operations for the Chaise Hub partner ecosystem.',
};

export default function ProTrackLayout({ children }) {
  return children;
}
